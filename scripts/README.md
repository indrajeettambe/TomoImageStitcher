# Scripts

Standalone helper scripts that do not fit elsewhere.

| File | Description |
|---|---|
| `make_release.sh` | Tag a new version, build the package, publish to PyPI. |
| `run_tests.sh`    | Run the full test suite. |
